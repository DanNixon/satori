[workspace]
resolver = "2"
members = [
  "common",
  "storage",

  "agent",
  "archiver",
  "ctl",
  "event-processor",
]

[workspace.package]
license = "MIT"
version = "0.2.0"
edition = "2021"

[workspace.dependencies]
mqtt-channel-client = { version = "0.6.0", features = ["metrics"] }
toml = "0.8"
